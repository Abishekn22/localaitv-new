---
name: LocalAI TV — App Build + Planning Track (continuity record)
description: Authoritative state for BOTH the live React-app modification work (hosted on Netlify) and the Plan-v1.3 planning track. Survives context compaction.
type: project
originSessionId: bdf68ed5-a922-4f9e-88a9-f62d59ad7ace
---

## A. LIVE APP — files, hosting, deploy workflow (current truth)

- **Canonical source (edit THIS):** `/Users/venkataswaraswamy/AI NEWS /App_v3_20260510_latest.jsx` (~20.7k lines, single-file React). The older `project_localaitv.md` "286.App_v3_…" canonical note is STALE — ignore it; use this file.
- **Preview wrapper:** `/Users/venkataswaraswamy/AI NEWS /preview_v3_20260510.html` — regenerated from source by a Python splice: replace `import { useState, useEffect, useRef, useCallback, useMemo } from "react";` → `const { … } = React;` and `export default AppRoot;` → `ReactDOM.createRoot(document.getElementById("root")).render(<AppRoot/>);`, sandwiched in the preview's HTML head + `<script type="text/babel">` … `</script></body></html>`.
- **Clean repo / deploy dir:** `~/localaitv-app-repo/` (App.jsx, preview.html, index.html=preview copy, splash-intro.mp4, home-page-header-logo.mp4, latest-logo.mp4, README.md, .gitignore). After each source edit: regenerate preview → `cp` preview→repo/preview.html & repo/index.html, source→repo/App.jsx.
- **GitHub (private):** `github.com/nagarjunak-pixel/localaitv-app-frontend` (auth via `gh` device-flow; account `nagarjunak-pixel`).
- **LIVE preview link (founder reviews here):** **https://localaitv-app-preview.netlify.app** — Netlify site id `35f74df6-0d25-49b7-9655-509766282758`, team `newd` (balajikamireddy9@gmail.com, **paid Personal plan** $9/mo so deploys work). Deploy cmd: `netlify deploy --dir "$HOME/localaitv-app-repo" --site 35f74df6-0d25-49b7-9655-509766282758 --prod` (netlify CLI at the localaitv-dev node bin; login persists).
- **Preview server (local verify):** Claude Preview config name `localaitv-preview` (port 8765, serves `~/AI NEWS /`). serverId changes per session — call preview_start with that name.
- **Verification method:** screenshot tool usually fails on this heavy app — verify via `preview_eval` DOM checks (read `#root` innerText, click elements by text, check iframe src / body class / errors). Babel compile of the ~1.4MB inline script takes ~14s after each reload — wait before checking.
- **App nav to reach forms:** pick constituency → Home → "మీ వార్తను…" upload CTA (or bottom-nav Upload FAB = the no-text center button between LIVE TV and LOCAL) → "🚀 Submit and Go To Upload" (registration gate) → tile (News/Birthdays/Marriages/Events/Jobs/Car-Motorcycle/Rentals/Shopping).

## B. App modifications COMPLETED & DEPLOYED (founder reviewing one-by-one)

Admin Dashboard demo (Profile→🛡️, role switcher, 15 modules) · constituency-routing fix (Home reads userConstituency, not hardcoded Kurnool) · Shopping form: + "Add Location/Street/Area" field, + "Contact Phone (Optional)", upload 1→3 files · "Shopping" added to Local categories + fixed Shopping→Jobs misroute + Shopping classifieds demo content · Vehicle form: + Fuel Type (Petrol/Diesel/CNG/EV), Year/KM/Color made mandatory(red *) · Events form: Organiser/Organisation made mandatory · Jobs form: renamed "Company/Shop/Establishment/Employer Name" + mandatory, + Qualification(opt) + Experience(opt) above Salary · **Dark Mode**: optional toggle replaced Search icon in Home header (default LIGHT, persists localStorage `localaitv_theme`; splash/intro/location are hardcoded-dark & unaffected; logo MP4 gated to light, SVG `<Logo dark>` adapts) · **Live TV audio**: home player `mute=1`→`mute=0` + `key={activeChannel.id}` + iframe gated `{(!feedViewer&&!selectedShort&&!showLiveOverlay)&&…}` so it's removed (audio hard-stops) when any overlay opens → single audio.
Honest caveat told to founder: iOS Safari may still need 1 tap for autoplay-with-sound (OS policy, not a bug). Screenshot-tool limitation disclosed each time.

## C. PLANNING TRACK — Plan v1.3 frozen, Phase 0 Validation Gate is POLICY

- `~/Desktop/localaitv-submission-kit/ADMIN-DASHBOARD-PLAN-v1.3.md` — **DO NOT EDIT.** Accepted fixes accumulate in **"Addendum A"** (now ~11+ items) → applied as v1.4 ONLY after the gate clears.
- **Gate (2026-05-15):** no Phase 1 / no coding / no v1.4 until Phase 0 proven on AWS (G1 NVENC <2min, G2 S3 upload, G3 CDN via content.localaitv.com, G4 e2e). Founder lifts the gate; nobody else. Still **HELD**.
- **Latest (2026-05-18 eve): `RESPONSE-Phase0-AWS-v2-and-Abishek-C1-C4-v1.3.md/.pdf`** — reviewed (a) revised `Phase0_AWS_Setup_Cost_Plan (1).docx` (now reports CDN/S3/ACM/CloudFront BUILT + G3 self-verified; DB cutover & GPU correctly HELD) and (b) `Abishek's C1–C4 Architecture Sign-off Answers_.pdf`. **P0 FLAG (open): team switched DNS from the accepted Hostinger-CNAME-only to AWS Route 53 A/AAAA alias — doc doesn't say if whole `localaitv.com` NS moved (would break live `aiservices.localaitv.com`) or only `content` subdomain delegated; demanded written live-app-safety confirmation before anything proceeds.** S3 sec config strong (BPA/OAC/SSE/versioning → closes S-2); access key vault-only (compliant). Abishek C1–C4 = technically sound but "answered-with-conditions", NOT clean sign-off (every section asks AI team for inputs; Gnana sign-off still separately required): C1 SRS-on-EC2 (SPOF risk), C2 CloudFront ~$385/mo Phase-1 figure is OPTIMISTIC (assumes ~7min/viewer/day; realistic ~1hr ⇒ ~$2.9k/mo), C3 start/stop adopted but Phase-1 9ch needs g4dn.12xlarge ≈ ~48 vCPU quota (8 vCPU is Phase-0 only), C4 keep pg_boss+PgBouncer (sound). **Cross-cutting decisive question raised to founder: batch scheduled-bulletins vs 24×7 live channels — 5–10× cost difference; founder to decide internally.** Gate STILL HELD. Prior `RESPONSE-Phase0-AWS-Setup-Cost-v1.3` superseded by this.
- Still-open gate-blockers: **DNS live-app safety (NEW P0)**, Admin-DB host verify, S-1 IAM scope, GPU quota approval, C1–C4 final (Abishek conditional + Gnana pending), monitoring/load/security tests. S-2 (bucket exists) now CLOSED.
- **AWS GPU quota timeline:** 16-May 16:41 AWS ack → 16-May 17:12 **auto-rejected** (templated "gradual ramp / avoid spikes", invited appeal); 18-May founder **reopened** w/ detailed use case (Case 177892985400464, separate acct 689186650531, 8 vCPU G/VT ap-south-1). 19-May produced `RESPONSE-AWS-GPU-Quota-Strategy-PlanA-PlanB` — diagnosis: new-acct/no-billing-history + Mumbai GPU scarcity + no Business Support. **Plan A** = upgrade AWS Business Support (biggest lever) + mature acct spend + softened Phase-0 wording + also file Spot & 2nd-region quotas. **Plan B** = run tiny Phase-0 GPU on RunPod/Lambda (or E2E Networks for India long-term) while keeping AWS S3/RDS/CloudFront; containerise GPU step for portability; temporary hybrid OK, permanent multi-cloud NOT advised. Phase-0 gate unaffected.
- RESPONSE docs produced (all in submission-kit, .md+.pdf): Sameer-v2, Gyan-DB-Spec, Gap-Analysis-v1.3, Production-Clarifications-v1.3, Abishek-Recommendations-v1.3, Architecture-Review-v1.3, Krishna-Clarifications-v1.3, Phase0-Prep-Completion-v1.3, Phase0-DB-CDN-v1.3, Phase0-AWS-Setup-Cost-v1.3, Phase0-AWS-v2-and-Abishek-C1-C4-v1.3, AWS-GPU-Quota-Strategy-PlanA-PlanB. Plus founder doc `LOCALAITV-DATA-STORAGE-FLOW-EXPLAINED.md/.pdf` (plain-language DB+storage walkthrough). Also FOUNDER-REQUIREMENTS, PHASE-0-RUNBOOK, PHASE-0-PREP-SCOPE-FOR-SAMEER-GNANA, HANDOFF.md. PDFs for founder viewing are copied to `~/Documents/` (pdf-viewer is sandboxed to ~/Documents).
- **People:** Founder = Koneti Mohan Reddy (non-technical; localaitv@gmail.com). CTO = Nagarjuna Reddy (handles registrations/credentials via Bitwarden vault — never to AI). AI pipeline engineers = Sheikh Sameer + Gnana Rajnan. Reviewers = Abishek N., Krishna (DevOps). NotebookLM = manual workflow (founder-confirmed). Founder dark-mode-accepted; reviewing pages one-by-one.

## D. PDF toolchain (reused every RESPONSE)

Node `~/Library/Application Support/localaitv-dev/node/bin/node` runs an ESM script importing `marked.esm.js` + `puppeteer-core` from `~/Desktop/localaitv-submission-kit/capacitor-project/node_modules/`, system Chrome. Write the .mjs fresh (sed breaks on `&`/apostrophes); keep the `title` string apostrophe/ampersand-free. CSS: A4, red #D0021B headers, navy table headers.

## E. Hard rules / boundaries

Never accept credentials (chat/file/screenshot/AnyDesk) — vault→Sameer/Gnana only. No v1.3 edits / no Phase-1 build until founder lifts the gate. App modifications ARE allowed (separate from the gated pipeline) — founder requests them directly, verify in browser, redeploy Netlify, report with honest caveats.
